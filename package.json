{
  "name": "@chatdakenh/widget",
  "version": "1.0.0",
  "description": "ChatDaKenh embeddable chat widget",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vite build && npx -y serve . -l 4200 --cors",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.25.4",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.4",
    "typescript": "^5.7.3",
    "vite": "^6.2.0"
  }
}
